# AI AGENT BEHAVIOR RULES - PJX PROJECT

## 🛑 MANDATORY FIRST STEP
**BEFORE** writing any code or analyzing the system, you **MUST** read:
👉 **`[Developer]/AGENT_SYSTEM_GUIDE.md`**

This document contains the modular architecture map and critical layout protocols that you are expected to follow.

## ⚠️ CRITICAL TECHNICAL RULES

1.  **Do NOT Use `base.css`**
    *   This file is deprecated/empty.
    *   Use `assets/system/css/base/variables.css` for design tokens (colors, spacing).

2.  **Maintain Topbar Alignment**
    *   To keep the Hamburger menu vertically centered, you must respect the **90px Min-Height** rule in `assets/system/css/layout/structure.css`.
    *   **NEVER** remove `min-height: 90px`, `display: flex`, or `align-items: center` from `.top-bar-wrapper`.

3.  **Modal Widths**
    *   Do NOT try to force modal widths via inline Styles alone.
    *   Use the whitelisting method in `assets/system/css/ui/modals.css`.

4.  **Inline Styles**
    *   Minimize usage of `style="..."` in JavaScript.
    *   Refactor complex styles into modular CSS files under `assets/system/css/systems/`.

## 📂 FILE SYSTEM NAVIGATION
*   **JS Logic:** `assets/system/js/` (Modular: ui, modal, system, utils)
*   **CSS Styles:** `assets/system/css/` (Modular: layout, screens, ui, systems)
*   **Config:** `assets/system/config.js` (Check this to see what's loaded)

---
*Generated by Antigravity - January 2026*
